{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "dev": "lerna run dev",
    "dev:backend": "lerna run --scope backend dev",
    "dev:frontend": "lerna run --scope frontend dev",
    "lint": "lerna run lint",
    "e2e": "lerna run test --scope frontend"
  },
  "dependencies": {
    "yarn": "^1.22.22"
  },
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
